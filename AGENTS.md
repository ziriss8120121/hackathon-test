# AGENTS.md

このrepoは、AI先行テスト用の `hackathon-test` です。
MBTI/心理機能を使った人狼シミュレーションを動かし、AI開発の進め方、実行環境の制約、ログ、結果画面を確認します。

## 正本

- Confluence: 目的、背景、判断、合意、次に何を変えるかの正本
- GitHub Markdown: 実装・実行に近い仕様、手順、ログ形式、結果の見方を書く場所
- GitHub: コード、実行設定、実行ログ、結果HTMLの正本
- `runs/`: 実験結果の実体

## ConfluenceとGitHub Markdownの使い分け

- Confluenceで合意した内容をGitHub Markdownへ反映する軽微な編集は、チームメンバーまたはAIエージェントが行ってよい。
- 後工程に影響しない表現整理、誤字修正、READMEの見やすさ改善、参照リンク追加、結果の見方の追記は、軽微な編集として扱う。
- MVP範囲、出力ファイル名、JSON/CSV項目、API、画面動作、実行方式、テスト条件に影響する変更は、ゆうじろう（Engineer）に確認する。
- Confluenceを更新する場合は、本文案をチャットに出して、依頼者またはページ責任者の確認を取ってから反映する。

## ファイル変更のルール

- 通常変更では、変更前に「どのファイルの」「どの箇所を」「どのように変更するか」をチャットで提示し、依頼者の同意を得てから実行する。
- 重要変更では、変更前にdiff形式の変更案をチャットに出し、依頼者の同意を得てから実行する。
- 重要変更には、`AGENTS.md`、Git運用ルール、要求定義、設計書、出力形式、画面仕様、コード、テスト、JSON/CSV項目、API、実行方式を含む。
- コード、テスト、JSON/CSV項目、API、実行方式に影響する変更は、必要に応じてゆうじろう（Engineer）に確認する。
- 変更後は、git diffとgit statusを確認し、変更内容を依頼者に確認させる。
- 依頼者が「OK」を出したら、git addしてcommitする。
- git push は絶対に実行しない。必ず人間が手動で行う。

## 基本ルール

- 作業前にREADMEと関連docsを読む。
- 変更前に、何を変えるかをチャットで短く説明する。
- mainブランチ上でファイル変更・commitを行わない。
- 作業前に必ず作業用branchを作成し、そのbranch上で変更する。
- mainへのpushは絶対に実行しない。
- APIキー、個人情報、課金情報をcommitしない。
- よく分からない大量削除、force push、resetはしない。
- ユーザーや他メンバーの変更を勝手に戻さない。

## 触る場所の目安

- しらまゆ（Biz）: `docs/`, `runs/`, 要求・結果確認
- ケンティー（Designer）: `docs/`, `playgrounds/mbti-werewolf/src/mbti_werewolf/web/static/`, 結果画面確認
- ゆうじろう（Engineer）: `playgrounds/mbti-werewolf/src/`, `tests/`, 実装全般

## 完了時の報告

完了時は以下を報告する。

- 何を変更したか
- 変更したファイル
- 実行・確認したこと
- 次に見るべき場所
