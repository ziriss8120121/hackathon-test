# AGENTS.md

このrepoは、AI先行テスト用の `hackathon-test` です。
AIエージェントは、このファイルを「作業前に確認する行動ルール」として扱います。

## Repoの目的

MBTI/心理機能を使った人狼シミュレーションを動かし、AI開発の進め方、実行環境の制約、ログ、結果画面を確認します。

## 情報の置き場

- Confluence: 目的、背景、判断、合意、次に何を変えるかの正本
- GitHub Markdown: 実装・実行に近い仕様、手順、ログ形式、結果の見方を書く場所
- GitHub: コード、実行設定、実行ログ、結果HTMLの正本
- `runs/`: 実験結果の実体

## 作業前にAIエージェントが確認すること

AIエージェントは、ファイル変更やGit操作の前に、必要に応じて以下を依頼者へ確認します。

- AIエージェントは、作業前に現在のbranchを確認します。
- mainブランチにいる場合は、ファイル変更やcommitを行わず、作業用branchの作成を提案して停止します。
- この変更は、実装コード、テスト、API、出力形式、実行方式に影響しますか？
- ゆうじろう（Engineer）の確認が必要ですか？
- Confluenceに関係する変更の場合、AIエージェントはConfluenceの正本を確認します。確認できない場合は、依頼者に確認して停止します。
- PRやmerge前に、AIエージェントは確認できる範囲でコンフリクト有無を確認します。GitHub画面などAIが確認できない場所は、依頼者に確認を依頼します。

## ファイル変更時の動き

- 通常変更では、変更前に「どのファイルの」「どの箇所を」「どのように変更するか」をチャットで提示し、依頼者の同意を得てから実行します。
- 重要変更では、変更前にdiff形式の変更案をチャットに出し、依頼者の同意を得てから実行します。
- 重要変更には、`AGENTS.md`、Git運用ルール、要求定義、設計書、出力形式、画面仕様、コード、テスト、JSON/CSV項目、API、実行方式を含みます。
- 変更後は、git diffとgit statusを確認し、変更内容を依頼者に確認させます。
- 依頼者が「OK」を出したら、git addしてcommitします。

## BranchとGit操作

- AIエージェントは、mainブランチ上でファイル変更・commitを行いません。
- AIエージェントは、作業前に作業用branchにいるか確認します。
- 作業用branchがない場合は、branch作成の提案をして、依頼者の同意を得てから作成します。
- AIエージェントは、git push を絶対に実行しません。pushは必ず人間が手動で行います。
- AIエージェントは、force push、reset、rebase、checkoutなど履歴や作業状態に強く影響する操作を勝手に行いません。

## PRとmerge判断の支援

- AIエージェントは、PRやmergeの可否を勝手に判断しません。
- AIエージェントは、PRやmerge前に、コンフリクト有無、変更内容、影響範囲、確認が必要な人を整理して報告します。
- コンフリクトがなく、後工程に影響しない軽微な変更に見える場合は、その判断材料を依頼者へ示します。
- 軽微な変更に見える場合でも、AIエージェントは「なぜ軽微と判断したか」を依頼者へ説明します。
- コンフリクト有無を確認できない場合は、merge可能とは判断せず、確認できない理由を報告します。
- 実装コード、テスト、API、出力形式、実行方式に影響する変更に見える場合は、ゆうじろう（Engineer）確認が必要か依頼者へ確認します。
- 判断に迷う場合は、merge前に作業を止めてチャットで確認します。

## コンフリクト時の動き

- AIエージェントは、コンフリクトを勝手に解消しません。
- コンフリクトがある場合は、どのファイルで何が衝突しているかを報告し、依頼者または担当者の判断を待ちます。
- コンフリクトがある状態では、merge、rebase、reset、checkoutを勝手に進めません。

## Confluence更新時の動き

- AIエージェントは、Confluenceを更新する前に本文案をチャットに出します。
- 依頼者またはページ責任者の確認を取ってから反映します。
- Confluenceで合意した内容をGitHub Markdownへ反映する軽微な編集は、後工程に影響しない範囲で行えます。
- MVP範囲、出力ファイル名、JSON/CSV項目、API、画面動作、実行方式、テスト条件に影響する場合は、実装影響がある変更として扱います。

## 触る場所の目安

- しらまゆ（Biz）: `docs/`, `runs/`, 要求・結果確認
- ケンティー（Designer）: `docs/`, `playgrounds/mbti-werewolf/src/mbti_werewolf/web/static/`, 結果画面確認
- ゆうじろう（Engineer）: `playgrounds/mbti-werewolf/src/`, `tests/`, 実装全般

## 禁止事項

- APIキー、個人情報、課金情報をcommitしません。
- ユーザーや他メンバーの変更を勝手に戻しません。
- よく分からない大量削除、force push、resetはしません。

## 完了時の報告

完了時は以下を報告します。

- 何を変更したか
- 変更したファイル
- 実行・確認したこと
- 次に見るべき場所
- PRを作成または更新した場合は、PRの状態（open / draft / merged）と、次に必要な操作（merge待ち、追加修正待ち、mainへpullなど）を報告する。
- merge後は、ローカルmainに戻ってpullが必要かを依頼者へ案内する。
